# Railway-specific environment configuration
# This helps Railway understand how to build and run the application

# Java version
java.runtime.version=17

# Build command
maven.custom.opts=-DskipTests

# Application port
server.port=${PORT:8080}
