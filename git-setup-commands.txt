# Commands to connect to GitHub (run these after creating GitHub repo)

# Connect to your GitHub repository
git remote add origin https://github.com/rjvs011/ecomService.git

# Push main branch
git push -u origin main

# Push dev branch
git push -u origin dev

# Set dev as default branch for development
git checkout dev
